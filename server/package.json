{
  "name": "server",
  "version": "1.0.0",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "start": "npx ts-node src/index.ts"
  },
  "dependencies": {
    "axios": "^1.13.6",
    "cors": "^2.x",
    "dotenv": "^16.x",
    "express": "^4.x",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "@types/express": "^4.x",
    "@types/node": "^20.x",
    "ts-node-dev": "^2.x",
    "typescript": "^5.x"
  }
}
